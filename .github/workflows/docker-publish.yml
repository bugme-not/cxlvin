name: Docker Build & Push
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Login
        uses: docker/login-action@v4
        with:
          username: ytrew777
          password: dckr_pat_ln1TWf4kopK2lcpH6cMlYO-u9Us
      - name: Build & Push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ytrew777/cxlvin:latest
